# 流水账

> append-only。每次摄入/沉淀/维护记一条,格式:
> `## [YYYY-MM-DD] <类型> | <摘要>`
>
> 常用类型:`ingest`(摄入沉淀) `setup`(建工具) `fix`(修问题)
> `lint`(体检) `learn`(学习) `capture`(跨项目回填) `correct`(修正此前判断)
>
> 为什么要记:**判断的演变本身有价值**。半年后你会想知道当时为什么那么决定。

